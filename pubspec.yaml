name: super_swipe
description: "A new Flutter project."

publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1

  # State Management
  flutter_riverpod: ^2.5.1

  # Navigation
  go_router: ^14.3.0

  # UI/Fonts
  google_fonts: ^6.2.1

  # Utilities
  cupertino_icons: ^1.0.8
  google_sign_in: ^6.2.1
  shared_preferences: ^2.5.3
  appinio_swiper: ^2.1.1
  cached_network_image: ^3.4.1
  flutter_cache_manager: ^3.4.1
  flutter_dotenv: ^6.0.0
  device_preview: ^1.3.1
  palette_generator: ^0.3.3+4
  cloud_firestore: ^5.6.12
  http: ^1.1.0
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.1

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/images/icons/swipe_logo.png"
  min_sdk_android: 21
  adaptive_icon_background: "#E8735A"
  adaptive_icon_foreground: "assets/images/icons/swipe_logo.png"
  web:
    generate: false
  windows:
    generate: false

flutter:
  uses-material-design: true
  assets:
    - .env
    - assets/images/
    - assets/images/icons/
